# Changesets

Every user-facing change ships with a changeset. Run `yarn changeset`, pick the bump
type, and describe the change from a consumer's point of view. The release workflow
turns accumulated changesets into a version PR and publishes to npm when it merges.
