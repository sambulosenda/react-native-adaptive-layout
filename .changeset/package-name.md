---
'react-native-adaptive-layout': minor
---

First release published as `react-native-adaptive-layout` (the name `react-native-foldable` belongs to an unrelated npm package). Install with `yarn add react-native-adaptive-layout`; exported API names are unchanged.
